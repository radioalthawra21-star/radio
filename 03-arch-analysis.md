# تحليل معماري: نظام دمج بيانات الحضور (تحليل فقط)

> ⚠️ تحذير: لا تقترح تعديل مباشر على بيانات جهاز البصمة أو att2000.mdb.  
> جميع التعديلات يجب أن تكون منطقية فقط (إضافة/إخفاء) على طبقة MongoDB.

---

## 1. هيكل النظام الحالي (Current Architecture)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  المصدر 1: جهاز ZKTeco (192.168.15.50:4370)                            │
│  - بصمات خام عبر TCP/node-zklib                                        │
│  - غير قابل للتعديل (read-only من منظورنا)                             │
│  - البيانات تتغير باستمرار (سجلات جديدة + قديمة تُحذف من ذاكرة الجهاز) │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  المصدر 2: att2000.mdb (Microsoft Access)                              │
│  ├── CHECKINOUT  (44,913 سجل) — نسخة محلية من بيانات الجهاز            │
│  ├── CHECKEXACT  (3 سجلات حالياً) — العمليات اليدوية                   │
│  │   └── ISADD=1, ISDELETE=1, ISMODIFY=1 (تعديل منطقي فقط)            │
│  └── USERINFO    (60 مستخدم) — يربط USERID ↔ BADGENUMBER (=zkUserId)   │
│  ═════════════════════════════════════════════════════════════════════   │
│  هذا هو Reference Source of Truth لأنه يحتوي البيانات الخام + التعديلات │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  المصدر 3: MongoDB Atlas (Attendance Model)                            │
│  - يحتوي فقط على البصمات القادمة من Bridge (الجهاز فقط)                │
│  - ✗ لا يحتوي على أي تعديلات من جهاز البصمة                        │
│  - هذه طبقة العرض النهائية (Output Layer)                              │
└─────────────────────────────────────────────────────────────────────────┘
```

### تدفق البيانات الحالي (به مشكلة)

```
جهاز ZKTeco ──TCP──► Bridge Service ──HTTP──► Express API ──► MongoDB ──► Website
                          ✗
                    لا يقرأ CHECKEXACT أبداً ← التعديلات اليدوية لا تصل للموقع
```

---

## 2. التدفق المطلوب (Target Data Flow)

```
┌─────────────────────────────────────────────────────────────────────┐
│  att2000.mdb  (Reference Source of Truth — يُقرأ فقط Read-Only)     │
│                                                                     │
│  CHECKINOUT ──────────┐                                             │
│  (44,913 سجل خام)     │                                             │
│                       ├──► MERGE ENGINE ◄── CHECKEXACT              │
│  USERINFO             │       (جديد)           (إضافة/إخفاء)        │
│  (USERID→BADGENUMBER)─┘                                             │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            ▼
              ┌──────────────────────────┐
              │  MongoDB (Output Layer)  │
              │  Attendance Model        │
              │  النتيجة النهائية بعد    │
              │  تطبيق قواعد الدمج       │
              └───────────┬──────────────┘
                          │
                          ▼
                    Website (مطابق 100% لـ جهاز البصمة)
```

**مبدأ أساسي:**
```
MongoDB = f(CHECKINOUT, CHECKEXACT, USERINFO)
حيث f هي: دمج + تحويل USERID→zkUserId + تطبيق الإخفاء والإضافة
```

---

## 3. قواعد الدمج (Merge Rules) — بالتفصيل

### 3.1 الاعتماديات والمفاتيح

| الجدول | المفتاح الأساسي | الملاحظة |
|--------|----------------|----------|
| CHECKINOUT | `(USERID, CHECKTIME, CHECKTYPE)` — مركب طبيعي | لا يوجد PK عمود واحد |
| CHECKEXACT | `EXACTID` (Auto-increment) | الربط مع CHECKINOUT عبر المفتاح الثلاثي |
| USERINFO | `USERID` | يربط USERID → BADGENUMBER (= zkUserId في النظام) |

### 3.2 القاعدة 1: الأساس — CHECKINOUT هو نقطة البداية

```
Result = CHECKINOUT  // كل سجلات الجهاز الخام (44,913 سجل)
```

لا يتم تعديل CHECKINOUT أبداً. هو المرجع للبيانات الأصلية.

### 3.3 القاعدة 2: تطبيق الإخفاء (ISDELETE)

```
لكل سجل D في CHECKEXACT حيث D.ISDELETE = 1:
    Result = Result - {
        USERID = D.USERID,
        CHECKTIME = D.CHECKTIME,
        CHECKTYPE = D.CHECKTYPE
    }
```

**شرح:**
- المفتاح الثلاثي `(USERID, CHECKTIME, CHECKTYPE)` يحدد السجل بدقة
- السجل الأصلي يبقى في CHECKINOUT — نحن فقط نخفيه من النتيجة النهائية
- إذا تكرر نفس المفتاح الثلاثي في CHECKINOUT (نادر)، يُحذف أول سجل فقط

**مثال:**
```
CHECKEXACT: USERID=5, CHECKTIME=10/06/2026 09:15:00, CHECKTYPE=I, ISDELETE=1
→ CHECKINOUT: USERID=5, CHECKTIME=10/06/2026 09:15:00, CHECKTYPE=I → يُستبعد من النتيجة
```

### 3.4 القاعدة 3: تطبيق الإضافة (ISADD)

```
لكل سجل A في CHECKEXACT حيث A.ISADD = 1:
    إذا (USERID, CHECKTIME, CHECKTYPE) غير موجود في Result:
        Result = Result + {A}
    وإلا:
        تجاهل A (السجل موجود مسبقاً — منع التكرار)
```

**شرح:**
- السجل المضاف غير موجود في الجهاز أصلاً (جاء من إدخال يدوي)
- التحقق من التكرار ضروري: قد يضيف المستخدم بصمة تطابق بالصدفة بصمة جهاز موجودة

**مثال:**
```
CHECKEXACT: USERID=1, CHECKTIME=10/06/2026 09:00:00, CHECKTYPE=I, ISADD=1
→ يضاف إلى النتيجة لأنه غير موجود في CHECKINOUT للمستخدم 1 في هذا التاريخ
```

### 3.5 القاعدة 4: التعديل (ISMODIFY) — النموذج المتوقع

```
النموذج المتوقع (يجب تأكيده باختبار يدوي):
عند تعديل بصمة في جهاز البصمة، يُنشئ سجلين في CHECKEXACT:
    1. سجل ISDELETE: بقيم OLD (USERID, CHECKTIME_OLD, CHECKTYPE_OLD)
    2. سجل ISADD: بقيم NEW (USERID, CHECKTIME_NEW, CHECKTYPE_NEW)

بالتالي: ISMODIFY لا يحتاج معالجة خاصة.
القاعدتين 2 و 3 تتعاملان معه تلقائياً:
    - السجل القديم يُستبعد (ISDELETE)
    - السجل الجديد يُضاف (ISADD)
```

**ما يجب اختباره:**
1. افتح جهاز البصمة
2. عدّل وقت بصمة موجودة (مثلاً من 9:00 إلى 8:30)
3. احفظ
4. افتح CHECKEXACT في SQL Server Management Studio أو اتصال ADODB
5. هل يظهر:
   - سجل ISDELETE للوقت القديم (9:00)؟
   - سجل ISADD للوقت الجديد (8:30)؟
   - أم يظهر سجل واحد ISMODIFY فقط؟

**احتمال بديل:** إذا كان جهاز البصمة يُعدّل CHECKINOUT مباشرة ويسجل ISMODIFY للتوثيق فقط، عندها:
- CHECKINOUT يحتوي القيمة المعدلة بالفعل
- لا نحتاج أي منطق إضافي — فقط نقرأ CHECKINOUT كما هو
- CHECKEXACT.ISMODIFY يصبح مجرد سجل تدقيق (Audit Log)

### 3.6 القاعدة 5: تحويل USERID → zkUserId → ObjectId

```
لكل سجل R في Result النهائي:
    zkUserId = USERINFO[R.USERID].BADGENUMBER

    User = MongoDB.User.findOne({ zkUserId: zkUserId })

    إذا User موجود:
        R.employee = User._id
        R.deviceUserId = zkUserId
    وإلا:
        R.employee = null
        R.deviceUserId = zkUserId  // للحين يتم الربط لاحقاً
        R.deviceUserName = USERINFO[R.USERID].Name  // اسم من Access
```

**معالجة المستخدمين غير المعرّفين:**
- يتم تخزين السجل مع `employee=null`
- يظهر في الموقع كـ "غير مرتبط"
- يمكن ربطه لاحقاً عبر واجهة Bulk Map Users

---

## 4. حالات التعارض (Conflict Scenarios)

### 4.1 تعارض التكرار (Duplication)

| # | السيناريو | مثال | آلية الكشف | الإجراء |
|---|-----------|------|------------|---------|
| 1 | CHECKINOUT + CHECKEXACT.ISADD لنفس (USERID, TIME, TYPE) | جهاز سجّل دخول 9:00، والمشرف أضاف نفس الدخول يدوياً | المفتاح الثلاثي `(USERID, CHECKTIME, CHECKTYPE)` موجود في Result | تجاهل ISADD — الأولوية للسجل الموجود (جهاز أو إضافة سابقة) |
| 2 | CHECKEXACT.ISADD مكرر (نفس EXACTID مختلف) | مشرف أضاف نفس البصمة مرتين | نفس المفتاح الثلاثي موجود من ISADD سابق | تجاهل المكرر، الاحتفاظ بالأول |
| 3 | CHECKEXACT.ISDELETE + CHECKINOUT لنفس السجل لكن أوقات مختلفة | المستخدم حذف بصمة 9:00، لكن هناك بصمة 9:01 بالصدفة | المفتاح الثلاثي يمنع التطابق الخاطئ | لا تضارب — المفتاح الثلاثي يضمن الدقة |
| 4 | جهاز يرسل بصمة عبر Bridge + CHECKEXACT يقرأ نفسها عبر Access | نفس البصمة تصل من مصدرين | `(zkUserId, CHECKTIME)` يجب أن يكون UNIQUE في MongoDB | استخدام upsert: إذا موجود → تجاهل، إذا لا → أضف |

### 4.2 تعارض التعديل (Modification Conflict)

| # | السيناريو | المشكلة | الحل |
|---|-----------|---------|------|
| 1 | بصمة جهاز 9:00، ISADD من جهاز البصمة 8:30 (نفس المستخدم والنوع) | أي المصدرين له الأولوية؟ | att2000.mdb هو المرجع — نحن نعكس ما يراه جهاز البصمة. إذا ISADD يضيف 8:30، الجهاز عنده 9:00، نضيف 8:30 ولا نحذف 9:00 (لأن ISADD لا يحذف، فقط يضيف). المشرف أراد إضافة بصمة وليس تعديل بصمة موجودة. |
| 2 | تعديل وقت بصمة (ISMODIFY) بعد أن تمت مزامنتها مع MongoDB بالفعل | السجل في MongoDB يحمل الوقت القديم | تطبيق القاعدة 4: حذف القديم (ISDELETE) + إضافة الجديد (ISADD) |
| 3 | حذف بصمة (ISDELETE) ثم الجهاز يعيد إرسالها عبر Bridge | تعود للظهور في الموقع | حلان: (أ) أولوية CHECKEXACT على الجهاز — إذا ISDELETE موجود، تجاهل بصمة الجهاز. (ب) مزامنة Access تحدد الـ Source of Truth |

**التوصية: الحل (أ) — CHECKEXACT له أولوية على الجهاز.**

### 4.3 تعارض الربط (Mapping Conflict)

| # | السيناريو | المشكلة | الحل |
|---|-----------|---------|------|
| 1 | USERID في CHECKEXACT ليس له BADGENUMBER في USERINFO | سجل بلا zkUserId، لا يمكن ربطه بموظف | تخزين مع `deviceUserId=USERID` + `deviceUserName=Name` + `employee=null`. يظهر في واجهة الربط لاحقاً |
| 2 | BADGENUMBER = 1069 (USERID=54) — لا يوجد مستخدم نظام بهذا zkUserId | السجلات تظهر مع employee=null | إما إنشاء مستخدم نظام جديد بـ zkUserId=1069، أو تجاهل السجلات مؤقتاً |
| 3 | BADGENUMBER مكرر في USERINFO (نادر) | أي USERID نربط؟ | أخذ أول USERID فقط + تسجيل تحذير |

### 4.4 تعارض المزامنة (Sync Timing Conflict)

| # | السيناريو | المشكلة | الحل |
|---|-----------|---------|------|
| 1 | بصمة جديدة على الجهاز لم تصل إلى att2000.mdb بعد (جهاز البصمة لم يزامن) | الموقع لا يراها إذا اعتمدنا فقط على att2000.mdb | الإبقاء على قراءة الجهاز المباشرة (Bridge) كمسار إضافي. الدمج النهائي: `Result = MAX(Bridge, MergeEngine)` — أي نأخذ السجل من المصدر الأحدث |
| 2 | جهاز البصمة يفتح att2000.mdb حصرياً | فشل قراءة الملف | فتح للقراءة فقط (`ReadOnly=true`). إذا فشل، الرجوع إلى قراءة الجهاز فقط (Fallback) |
| 3 | تزامن دوري لـ CHECKINOUT من الجهاز إلى Access (يقوم به جهاز البصمة) | بيانات Access قديمة (تأخير دقائق/ساعات) | هذا طبيعي — البصمات الجديدة تصل عبر Bridge فوراً، والتعديلات اليدوية تصل عبر Access عند توفرها |

---

## 5. استراتيجية المزامنة الآمنة (Safe Sync Strategy)

### 5.1 المبادئ الأساسية

```
1. Read-Only على att2000.mdb — لا نكتب عليه أبداً
2. Read-Only على الجهاز — لا نعدل البصمات الأصلية
3. MongoDB هو الـ Output Layer الوحيد القابل للكتابة
4. CHECKEXACT هو المرجع للتعديلات اليدوية (له أولوية على الجهاز)
5. كل عملية مزامنة تُسجّل في سجل التدقيق (Audit Log)
```

### 5.2 خطوات المزامنة الدورية

```
المزامنة كل N دقيقة (قابلة للتكوين، افتراضي: 5 دقائق):

المرحلة 1 — قراءة att2000.mdb:
  1.1. قراءة CHECKINOUT (فقط السجلات الجديدة منذ آخر مزامنة)
  1.2. قراءة CHECKEXACT (جميع السجلات — عددها صغير)
  1.3. قراءة USERINFO (USERID → BADGENUMBER)

المرحلة 2 — الدمج:
  2.1. تطبيق القاعدة 1: Result = CHECKINOUT
  2.2. تطبيق القاعدة 2: استبعاد ISDELETE من Result
  2.3. تطبيق القاعدة 3: إضافة ISADD إلى Result (مع منع التكرار)
  2.4. تطبيق القاعدة 4: معالجة ISMODIFY (حسب النموذج المُختبر)
  2.5. تطبيق القاعدة 5: تحويل USERID → zkUserId → ObjectId

المرحلة 3 — المزامنة مع MongoDB:
  3.1. لكل سجل في Result:
      - إذا موجود في MongoDB: قارن البيانات → حدّث إذا اختلفت
      - إذا غير موجود: أنشئ سجلاً جديداً
  3.2. لكل سجل في MongoDB ليس في Result:
      - إذا كان مصدره الجهاز (source=bridge): احذفه (تم حذفه أو إخفاؤه)
      - إذا كان مصدره يدوي (source=manual): احتفظ به (تعديل من النظام نفسه)

المرحلة 4 — التسجيل:
  4.1. سجل عملية المزامنة: عدد السجلات المضافة/المحدّثة/المحذوفة
  4.2. سجل أي أخطاء: فشل قراءة، فشل ربط، تضارب
```

### 5.3 التعامل مع Bridge — المسار المزدوج

```
المشكلة:
  - Bridge يقرأ من الجهاز مباشرة (real-time)
  - att2000.mdb يحتوي التعديلات لكنه متأخر عن الجهاز

الحل — مساران متوازيان:

المسار 1 (Real-time):  الجهاز → Bridge → MongoDB (بصمات جديدة فوراً)
المسار 2 (دوري):       att2000.mdb → Merge Engine → MongoDB (تعديلات + بصمات معدلة)

قاعدة الدمج النهائي:
  لكل (employee, date) في MongoDB:
    - احتفظ بسجلات المسار 1 (الجهاز)
    - إذا جاء سجل من المسار 2 يطابق نفس (employee, date, checkTime):
        - سجل المسار 2 له الأولوية (لأنه يحتوي التعديلات اليدوية)
    - إذا جاء ISDELETE من المسار 2:
        - احذف السجل المطابق (حتى لو كان من الجهاز)

مبدأ الأولوية:
  CHECKEXACT.ISDELETE > بصمة الجهاز (المسار 1)
  CHECKEXACT.ISADD > بصمة الجهاز (لنفس الوقت والمستخدم)
  بصمة الجهاز الجديدة (المسار 1) > لا شيء (تضاف كالمعتاد)
```

### 5.4 منع التكرار — Deduplication Strategy

```
على مستوى MongoDB:
  المفتاح الفريد: { employee, date, checkIn.time }
  (يجب إضافة Unique Index على (employee, date) أو (employee, date, checkIn.time))

على مستوى الدمج:
  لكل سجل يُضاف إلى Result:
    تحقق: هل (zkUserId, CHECKTIME, CHECKTYPE) موجود مسبقاً؟
    إذا نعم: تجاهل (استثناء: إذا كان CHECKEXACT.ISADD، له أولوية على CHECKINOUT)

على مستوى المزامنة:
  استخدم upsert: findOneAndUpdate مع upsert: true
  المفتاح: { employee: ObjectId, date: dayStart }
```

### 5.5 إستراتيجية معالجة الأخطاء

```
┌─ مستوى الخطأ ────────────────┬─ الإجراء ──────────────────────────────┐
│ تعذر فتح att2000.mdb         │ الرجوع إلى قراءة الجهاز فقط (Fallback) │
│ (مقفول من جهاز البصمة)   │ المحاولة مرة أخرى بعد 30 ثانية        │
├──────────────────────────────┼────────────────────────────────────────┤
│ USERID غير موجود في USERINFO │ تسجيل تحذير، تجاهل السجل (أو device)  │
│ (بيانات تالفة)              │                                       │
├──────────────────────────────┼────────────────────────────────────────┤
│ BADGENUMBER لا يطابق         │ تخزين مع employee=null, deviceUserId  │
│ أي zkUserId في النظام       │ + تسجيل في قائمة "غير معرّفين"       │
├──────────────────────────────┼────────────────────────────────────────┤
│ مفتاح ثلاثي مكرر            │ تجاهل المكرر، الاحتفاظ بالأول          │
├──────────────────────────────┼────────────────────────────────────────┤
│ فشل اتصال MongoDB            │ إعادة المحاولة 3 مرات، ثم إيقاف        │
│                              │ المزامنة والتسجيل في error log        │
└──────────────────────────────┴────────────────────────────────────────┘
```

---

## 6. خريطة التنفيذ المقترحة

### المرحلة 0 — اختبار ميداني إلزامي (قبل أي كود)

```
□ اختبار ISMODIFY:
   1. افتح جهاز البصمة
   2. عدّل وقت بصمة موجودة
   3. تفريغ CHECKEXACT
   4. هل يظهر: (ISDELETE + ISADD) أم (ISMODIFY فقط)؟

□ اختبار ISDELETE:
   1. احذف بصمة من جهاز البصمة
   2. تفريغ CHECKEXACT
   3. هل يظهر ISDELETE مع المفتاح الثلاثي الصحيح؟

□ اختبار Open att2000.mdb للقراءة:
   1. مع تشغيل جهاز البصمة
   2. حاول فتح الملف للقراءة فقط عبر ADODB/PowerShell
   3. هل ينجح الاتصال؟
```

### المرحلة 1 — بناء قارئ att2000.mdb

```
المكونات:
  - read-att2000.js (يقرأ CHECKINOUT, CHECKEXACT, USERINFO)
  - user-mapper.js (USERID → BADGENUMBER → zkUserId → ObjectId)
  - حالة: PowerShell SDK Bridge (موجود) أو ADODB مباشر
```

### المرحلة 2 — بناء Merge Engine

```
المكونات:
  - merge-logic.js (يطبق القواعد 1-5)
  - وحدة منع التكرار
  - وحدة معالجة التعارض
  - سجل التدقيق لكل عملية دمج
```

### المرحلة 3 — التكامل مع MongoDB

```
المكونات:
  - mongo-sync.js (مقارنة + إدراج + تحديث + حذف)
  - إضافة Unique Index (employee + date) إذا لم يكن موجوداً
  - معالجة upsert للبيانات المكررة
```

### المرحلة 4 — التكامل مع Bridge

```
الدمج:
  - إضافة مرحلة مزامنة Access إلى Bridge/index.js
  - تحديد تردد المزامنة
  - إضافة Fallback عند فشل Access
```

---

## 7. الملخص التنفيذي

| البند | القرار |
|-------|--------|
| Source of Truth | `att2000.mdb` (لأنه يحتوي بيانات الجهاز + التعديلات) |
| هل نعدل الجهاز؟ | ❌ ممنوع — قراءة فقط |
| هل نعدل att2000.mdb؟ | ❌ ممنوع — قراءة فقط (`ReadOnly=true`) |
| أين نكتب؟ | ✅ فقط على MongoDB |
| أولوية CHECKEXACT | أعلى من الجهاز — التعديل اليدوي يعلو على البصمة الخام |
| مفتاح الربط | `(USERID, CHECKTIME, CHECKTYPE)` في Access، ثم `(zkUserId, date)` في MongoDB |
| ISMODIFY | يتطلب اختباراً يدوياً — النموذج المتوقع: ISDELETE + ISADD |
| المستخدم غير المعروف | employee=null + deviceUserId + يظهر لاحقاً للربط |
| التكرار | يُمنع بالمفتاح الفريد `(employee, date)` في MongoDB |
| التعارض | CHECKEXACT له الأولوية على الجهاز |
| تردد المزامنة | كل 5 دقائق (قابل للتعديل) |
| Fallback | إذا فشل Access → الرجوع إلى قراءة الجهاز فقط |

---
*تاريخ التحليل: 14 يونيو 2026*
