# تقرير الملفات غير المستخدمة — Unused Files Report

**منهجية الإثبات:** تم تتبع كل ملف — هل يستورده (import/require) أي ملف آخر في المشروع؟  
البحث شمل جميع ملفات `.js`, `.jsx`, `.ts`, `.tsx`, `.mjs`, `.cjs`, `.json`, `.yml`, `.yaml` في `backend/`, `frontend/`, `bridge/`, `SDK/`, وجذر المشروع.  
استُبعدت المجلدات: `node_modules/`, `graphify-out/`.

---

## 🟢 القسم الأول: Safe To Remove

هذه الملفات لا يؤثر حذفها على تشغيل التطبيق إطلاقاً.  
لا يوجد أي `import/require` أو `exec/spawn/fork` أو مرجع تكويني يشير إليها.

---

### 1. مجلد `backend/scripts/` — 30 ملفاً (بالكامل)

**دليل الإثبات:** `grep -r "require.*scripts/"` → 0 نتيجة.  
**`package.json` scripts:** لا يوجد سكربت يشير إلى أي ملف فيه.  
**`server.js`:** لا يستورد أي ملف منه.

| الملف | الحجم |
|-------|-------|
| `backend/scripts/_check_db.js` | صغير |
| `backend/scripts/_find_dups.js` | صغير |
| `backend/scripts/_list_mapped.js` | صغير |
| `backend/scripts/backup.js` | متوسط |
| `backend/scripts/check-api-status.ps1` | صغير |
| `backend/scripts/check-today.mongo.js` | صغير |
| `backend/scripts/cleanup-empty-records.js` | صغير |
| `backend/scripts/diagnose-and-repair.js` | كبير |
| `backend/scripts/diagnose-dates.js` | متوسط |
| `backend/scripts/diagnose-records.js` | كبير |
| `backend/scripts/dump-raw-attendance.js` | كبير |
| `backend/scripts/force-sync-all.js` | كبير |
| `backend/scripts/inspect-raw-timestamps.js` | متوسط |
| `backend/scripts/list-users.js` | صغير |
| `backend/scripts/migrate-images.js` | متوسط |
| `backend/scripts/migrate-to-prod.js` | متوسط |
| `backend/scripts/raw-socket-test.js` | متوسط |
| `backend/scripts/raw-tcp-test.js` | متوسط |
| `backend/scripts/raw-udp-test.js` | صغير |
| `backend/scripts/resetAdminPassword.js` | صغير |
| `backend/scripts/seed-test-data.js` | كبير |
| `backend/scripts/smart-sync-today.js` | متوسط |
| `backend/scripts/suggest-mappings.js` | كبير |
| `backend/scripts/sync-targeted-today.js` | كبير |
| `backend/scripts/test-notify-manager.js` | متوسط |
| `backend/scripts/test-packet-variations.js` | كبير |
| `backend/scripts/try-connection.js` | متوسط |
| `backend/scripts/updateHRUser.js` | متوسط |
| `backend/scripts/verifyAdmin.js` | صغير |

---

### 2. ملفات `backend/` root — سكربتات فحص واختبار (18 ملفاً)

**دليل الإثبات:** `grep -r "require.*check_api\|require.*test-login" ` → 0 نتيجة.

| الملف |
|-------|
| `backend/check_api.js` |
| `backend/check_api2.js` |
| `backend/check_attendance_temp.js` |
| `backend/check_mongo.js` |
| `backend/check_mongo2.js` |
| `backend/check_mongo3.js` |
| `backend/check_today.js` |
| `backend/check-attendance.js` |
| `backend/check-device.js` |
| `backend/check-roles.js` |
| `backend/debug-users.js` |
| `backend/fix-departments.js` |
| `backend/inspect-role-chars.js` |
| `backend/migrate-departments.js` |
| `backend/seed-depts.js` |
| `backend/verify-models.js` |
| `backend/test-admin-update.js` |
| `backend/test-login.js` |
| `backend/test-mustafa-login.js` |
| `backend/test-upload.js` |
| `backend/tests/compensationService.test.js` |

---

### 3. مجلد `ZKTeco/` — برنامج ZKTeco القديم (كامل)

**دليل الإثبات:**  
- `grep -r "ZKTeco/"` — النتائج تشير فقط إلى اسم الجهاز كـ label نصي (`ZKTeco_192.168.1.201`) وليس إلى مسار الدليل.
- لا يوجد `require('../ZKTeco/...')` أو `import` في أي ملف.
- لا يوجد مرجع في `package.json`, `docker-compose.yml`, أو أي ملف إعدادات.

**المحتوى:** برنامج سطح مكتب ZKTeco Att.exe + قواعد بيانات Access + تقارير Crystal Reports + تعريفات USB + أدوات ADB.

| المسار | الوصف |
|--------|-------|
| `ZKTeco/Att.exe` | تطبيق سطح المكتب القديم |
| `ZKTeco/Att.chm` | ملف تعليمات |
| `ZKTeco/Att.dat`, `Att.log` | ملفات بيانات |
| `ZKTeco/att2000.mdb`, `att2000.ldb` | قواعد بيانات Access |
| `ZKTeco/*.rpt` (15 ملفاً) | تقارير Crystal Reports |
| `ZKTeco/FR_Chs.dll`, `FR_Eng.dll`, `midas.dll`, إلخ | مكتبات DLL |
| `ZKTeco/zkecap.exe`, `zkeupg.exe`, `rptviewer.exe` | أدوات |
| `ZKTeco/adb/` | Android Debug Bridge بالكامل |
| `ZKTeco/driver/` | تعريفات libusb للبصمة |
| `ZKTeco/USBDriver/` (5 مجلدات) | تعريفات USB |

---

### 4. مجلد `backups/` — نسخ احتياطية قديمة

| الملف |
|-------|
| `backups/restore-2026-06-09_14-21/attendances_recent.json` |
| `backups/restore-2026-06-09_14-21/biometricerrorlogs.json` |
| `backups/restore-2026-06-09_14-21/devicelogs.json` |
| `backups/restore-2026-06-09_14-21/users.json` |

**ملاحظة:** إذا كانت هذه النسخة مهمة، يمكن نقلها خارج المشروع قبل الحذف.

---

### 5. سكربتات SDK تجريبية (3 ملفات)

| الملف |
|-------|
| `SDK/test_sdk.ps1` |
| `SDK/test_sdk2.ps1` |
| `SDK/test_records.ps1` |

**ملاحظة:** السكربت الفعلي المستخدم هو `SDK/SDKBridge/SDKBridge.ps1` — وهذا يبقى كما هو.

---

### 6. ملفات تجريبية في `backend/utils/` (2 ملف)

| الملف |
|-------|
| `backend/utils/test_puppeteer.cjs` |
| `backend/utils/test_output.pdf` |

---

### 7. ملفات سجل (Log Files) — 5 ملفات

| الملف |
|-------|
| `backend/backend.log` |
| `backend/server.log` |
| `backend/server.err` |
| `backend/server_err.log` |
| `backend/server_out.log` |

---

### 8. ملفات `backend/public/` — صفحة HTML غير مرجعية

| الملف | الإثبات |
|-------|---------|
| `backend/public/vba-download.html` | لا يوجد أي ملف `.js`/`.jsx`/`.ts`/`.tsx` يشير إليه باسمه. يتم serve passively عبر `express.static` في السطر 242 من `server.js`، ولكن لا يوجد رابط له في أي كود. |

**ملاحظة:** `backend/public/supervisor.html` **مستخدم فعلاً** — يبقى.  
هذا الملف فقط (`vba-download.html`) غير مرجع.

---

### 9. ملفات التوثيق المنفصلة — 8 ملفات

| الملف |
|-------|
| `backend/docs/SCHEMA.md` |
| `00-الدستور.md` (root) |
| `00-خلاصة.md` (root) |
| `01-merge-spec.md` (root) |
| `02-arch-design.md` (root) |
| `03-arch-analysis.md` (root) |
| `04-analysis-report.md` (root) |
| `PROJECT_STRUCTURE.txt` (root) |

**ملاحظة:** `AGENTS.md` يبقى — هو ملف تعليمات لـ opencode.

---

### 10. ملفات جذر المشروع المنعزلة — 14 ملفاً

| الملف | الإثبات |
|-------|---------|
| `root/index.html` | `server.js` يخدم `frontend/dist/index.html` وليس هذا الملف |
| `mogo.txt` | غير مرجع من أي كود |
| `all_files.csv` | مدرج في `.gitignore` |
| `audit-logs-report.txt` | تقرير قديم |
| `CORS_FIX.patch` | ملف Patch فارغ |
| `debug.log` | Chrome crashpad log |
| `stderr.txt` | output مأسور |
| `stdout.txt` | output مأسور |
| `0ملون.png` | صورة غير مستخدمة |
| `بدون عنوان.png` | صورة غير مستخدمة |
| `برومت اوامر للبرمجة.zip` | مضغوط، مدرج في `.gitignore` |
| `start-tunnel.ps1` | سكربت مستقل، مدرج في `.gitignore` |
| `remove-bom.js` | (منقول إلى Needs Verification — راجع القسم التالي) |
| `backend/مستند نصي جديد.txt` | ملف نصي فارغ |

---

## 🟡 القسم الثاني: Needs Verification

هذه الملفات تحتاج مراجعة يدوية قبل الحذف — قد تكون ضرورية لسبب غير ظاهر من الـ imports وحدها.

| الملف | لماذا يحتاج تحقق؟ | ماذا تفعل؟ |
|-------|-------------------|------------|
| `root/remove-bom.js` | مرجع من `package.json` scripts (`"remove-bom": "node remove-bom.js"`) — إذا كان أحد يشغّل هذا السكربت، سيختفي. | تحقق إن كان أحد يستخدم `npm run remove-bom` |
| `bridge/.env.example` | ملف مثال للإعدادات — مفيد للمطورين الجدد. | انقله خارج المشروع وليس حذفاً |
| `frontend/.env` | فارغ تقريباً — لكن قد يُستخدم مستقبلاً. | تحقق من محتواه أولاً |
| `frontend/.env.local` | ملف ثنائي/فارغ. | تحقق من محتواه |
| `frontend/netlify.toml` | إعدادات نشر Netlify — قد يكون نشطاً أو من بقايا تجربة سابقة. | تحقق إن كان النشر على Netlify نشطاً |
| `docker-compose.yml` | ملف إعداد Docker — قد لا يكون مستخدماً ولكن له قيمة توثيقية. | تحقق إن كان أحد يستخدم Docker |
| ملفات التوثيق العربية (root) | `00-الدستور.md`, `00-خلاصة.md`, `01-merge-spec.md`, `02-arch-design.md`, `03-arch-analysis.md`, `04-analysis-report.md` — قد تحتوي على تحليلات معمارية مهمة. | راجع المحتوى أولاً، انقل إلى `/docs` إذا لزم الأمر |
| ملفات `.txt` العربية (root) | `آلية_ربط_جهاز_ZKTeco.txt`, `البصمة.txt`, `برومت المستودعين المنفصلين .txt`, `خطوات_تشغيل_نظام_البصمة.txt`, `مخاطر.txt`, `ztk نصي.txt` — توثيق ZKTeco. | راجع المحتوى، انقل إلى `/docs` |
| ملفات `.md`/`.txt` إضافية (root) | `PROJECT_STRUCTURE.txt`, `audit-logs-report.txt` — قد تكون مرجعية. | راجع المحتوى |

---

## 📊 ملخص إحصائي

| الفئة | العدد | الحجم التقريبي |
|-------|-------|---------------|
| 🟢 **Safe To Remove** | **~80-90 ملفاً** | **~40-60MB** |
| 🟡 **Needs Verification** | **~15-20 ملفاً** | **~2-5MB** |
| ✅ **Files that stay** (جميع ملفات التطبيق الفعلية) | ~200 ملف | — |

**تفاصيل Safe To Remove:**
- `backend/scripts/` — 30 ملفاً
- `backend/` root check/test/debug — 21 ملفاً
- `ZKTeco/` — ~50 ملفاً
- `backups/` — 4 ملفات
- `SDK/` test scripts — 3 ملفات
- `backend/utils/` test — 2 ملفات
- `backend/logs` — 5 ملفات
- `backend/public/vba-download.html` — 1 ملف
- `backend/docs/SCHEMA.md` — 1 ملف
- Root stray — ~14 ملفاً

---

## ⚠️ تنبيهات مهمة

1. **`CheckExact.js`** في `backend/models/` — **مستخدم فعلاً** (خطأ في التقرير السابق). يستورده `supervisorController.js` في 10 مواقع. لا تحذفه.

2. **`backend/public/supervisor.html`** — **مستخدم فعلاً**. يخدمه `server.js` (سطر 242) ويستدعيه `TempSupervisor.jsx`. لا تحذفه.

3. **`backend/scripts/resetAdminPassword.js`** — مصنف Safe To Remove لأنه لا يستورده أي كود، لكن إذا كنت تحتاج إعادة تعيين كلمة المرور يدوياً، احتفظ به خارج المشروع.

4. **المجلدات التي تم تخطيها (لن تُحذف):**
   - `backend/` — كود التطبيق الأساسي
   - `frontend/` — كود الواجهة الأمامية
   - `bridge/` — خدمة ZKTeco bridge
   - `SDK/SDKBridge/` — السكربت الفعلي المستخدم (`SDKBridge.ps1`)
   - `SDK/zkemkeeper.dll` و `SDK/*.bat` — ملفات الـ SDK الأساسية
   - `backend/models/` — جميع الموديلات (كلها مستخدمة)
   - `backend/controllers/` — جميع المتحكمات (كلها مستخدمة)
   - `backend/services/` — جميع الخدمات (كلها مستخدمة)
   - `backend/routes/` — جميع المسارات (كلها مستخدمة)
   - `graphify-out/` — بيانات المعرفة الرسومية
