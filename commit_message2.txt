feat: Remove custom Cairo font, use system fonts only

- Remove Cairo font from Google Fonts list in TypographyContext
- Change DEFAULT_FONTS to use system UI font stack (Windows/Linux/Mac)
- Remove preloaded 'CAIRO' from loadedFonts state
- Update DeveloperPanel.jsx:
  * Change default font family in DEFAULT_SETTINGS to system UI stack
  * Remove 'CAIRO' from FONT_FAMILIES list
- Update TypographySettings.jsx:
  * Remove all font-family inline styles (rely on CSS variables)
  * Empty GOOGLE_FONTS list means no Google font options in dropdown
- Update index.css:
  * Change --font-heading and --font-body to system UI font stack
  * Update .english-numerals to use system UI font stack

Result: 
- All text now uses the system default font (Segoe UI on Windows, etc.)
- Users can still upload custom fonts via Developer Panel
- No more dependency on Google Fonts for Cairo
- Better performance and offline capability
