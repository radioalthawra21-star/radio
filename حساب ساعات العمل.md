# حساب ساعات العمل - توثيق شامل

## 1. إعدادات ساعات الدوام الرسمي

### المصدر: `backend/models/Settings.js`
| المفتاح | القيمة | الوصف |
|---|---|---|
| `workStartHour` | 9 | ساعة بدء الدوام (09:00 صباحاً) |
| `workStartMinute` | 0 | دقيقة بدء الدوام |
| `workEndHour` | 16 | ساعة نهاية الدوام (04:00 مساءً) |
| `workEndMinute` | 0 | دقيقة نهاية الدوام |
| `dailyWorkHours` | 7 | عدد ساعات العمل اليومية (9-4 = 7 ساعات) |
| `lateGracePeriodMinutes` | 0 | فترة سماح التأخير (دقائق) |
| `earlyLeaveGracePeriodMinutes` | 0 | فترة سماح الخروج المبكر (دقائق) |
| `veryLateThresholdMinutes` | 120 | حد التأخير الكبير (دقيقتان = يُعتبر متأخر جداً) |

### التحويل للتوقيت العالمي (UTC)
لأن السعودية UTC+3:
- 09:00 صباحاً سعودي = **06:00 UTC**
- 04:00 مساءً سعودي = **13:00 UTC**

---

## 2. هيكل سجل الحضور (Attendance Model)

### المصدر: `backend/models/Attendance.js`

| الحقل | النوع | الوصف |
|---|---|---|
| `employee` | ObjectId → User | الموظف (مطلوب) |
| `date` | Date | تاريخ اليوم (مطلوب) |
| `checkIn.time` | Date | وقت تسجيل الدخول |
| `checkIn.status` | String | `on_time` / `late` / `very_late` |
| `checkIn.location` | String | مكان التسجيل |
| `checkIn.notes` | String | ملاحظات |
| `checkOut.time` | Date | وقت تسجيل الخروج |
| `checkOut.status` | String | `on_time` / `early` |
| `checkOut.location` | String | مكان التسجيل |
| `checkOut.notes` | String | ملاحظات |
| `duration` | Number | إجمالي ساعات التواجد (بين أول وآخر بصمة) |
| `expectedHours` | Number | ساعات العمل المتوقعة (default: 7) |
| `overtime` | Number | ساعات إضافية |
| `status` | String | `present` / `absent` / `late` / `half_day` / `on_leave` / `work_from_home` |
| `leave` | ObjectId → LeaveRequest | مرجع للإجازة إن وجدت |
| `isApproved` | Boolean | حالة الموافقة |

### الحالات (Enum)
```
AttendanceStatus {
  PRESENT:      'present',
  ABSENT:       'absent',
  LATE:         'late',
  HALF_DAY:     'half_day',
  ON_LEAVE:     'on_leave',
  WORK_FROM_HOME: 'work_from_home'
}
```

---

## 3. مسارات حساب المدة والإضافي

### 3.1 المزامنة من جهاز البصمة (ZKTeco)

#### المصدر: `backend/controllers/zktecoController.js`

هناك **3 دوال مزامنة** تستخدم نفس منطق الحساب:

1. `receiveAttendance` (سطر 68) - تستقبل بيانات من bridge
2. `syncDeviceAttendance` (سطر 293) - مزامنة مباشرة مع الجهاز
3. `cleanSyncDeviceAttendance` (سطر 654) - تنظيف وتزامن

#### أ. تجميع البصمات لكل موظف لكل يوم
```
group.timestamps = [timestamp1, timestamp2, ...]  // كل بصمات الموظف لليوم
group.timestamps.sort((a, b) => a - b)

checkInTime  = group.timestamps[0]                                       // أول بصمة
checkOutTime = group.timestamps[group.timestamps.length - 1]            // آخر بصمة
               (if length > 1 AND difference > 60000ms = 1 دقيقة)
               else null                                                 // لا يوجد خروج
```

#### ب. حساب الحالة
```
hasCheckOut = checkOutTime !== null

attendanceStatus = hasCheckOut
  ? (checkInStatus !== ON_TIME ? LATE : PRESENT)
  : HALF_DAY
```

#### ج. حساب المدة والإضافي (تم تصحيحه)
يُستخدم `calcDurationOvertime(checkInTime, checkOutTime)` (سطر 55):

```
المدة = (checkOutTime - checkInTime) / (1000 * 60 * 60)
      = إجمالي الساعات بين أول وآخر بصمة

// حدود الدوام بالتوقيت العالمي (UTC):
workStartMin = 6 * 60 = 360   // 06:00 UTC = 09:00 سعودي
workEndMin   = 13 * 60 = 780  // 13:00 UTC = 16:00 سعودي

// تحويل أوقات البصمة لدقائق UTC:
checkInMin  = checkInTime.getUTCHours() * 60 + checkInTime.getUTCMinutes()
checkOutMin = checkOutTime.getUTCHours() * 60 + checkOutTime.getUTCMinutes()

// الإضافي: الوقت قبل بداية الدوام + الوقت بعد نهاية الدوام
overtimeAfter  = max(0, checkOutMin - workEndMin)     // دقائق بعد 4:00 م
overtimeBefore = max(0, workStartMin - checkInMin)    // دقائق قبل 9:00 ص
overtime = (overtimeAfter + overtimeBefore) / 60      // تحويل لساعات
```

**مثال:** دخول 4:44 صباحاً، خروج 23:15 مساءً:
```
المدة = 23:15 - 4:44 = 18.52 ساعة
overtimeAfter  = max(0, 1395 - 780) = 615 دقيقة (10.25 ساعة)
overtimeBefore = max(0, 360 - 284)  = 76 دقيقة  (1.27 ساعة)
الإضافي = (615 + 76) / 60 = 11.52 ساعة
```

#### د. تحديد حالة تسجيل الدخول
`determineCheckInStatus(timestamp)` (سطر 40):
```
workStart = 09:00 سعودي

if checkInTime <= 09:00 → ON_TIME
if checkInTime > 09:00 by 0-120 min → LATE
if checkInTime > 09:00 by 120+ min → VERY_LATE
```

---

### 3.2 تسجيل الحضور يدوياً (من النظام)

#### المصدر: `backend/controllers/attendanceController.js`

#### أ. تسجيل دخول (`checkIn` - سطر 16)
```
// يجلب الإعدادات:
workStartHour, workStartMinute, dailyWorkHours, lateGracePeriod, veryLateThreshold

// ينشئ أو يحدث سجل الحضور:
attendance.checkInEmployee(currentTime, location, notes, {
  workStartHour, workStartMinute,
  lateGracePeriodMinutes, veryLateThresholdMinutes
})
```

**`checkInEmployee` في `Attendance.js` (سطر 175):**
```
workStart = setHours(workStartHour, workStartMinute)
graceEnd = workStart + lateGracePeriodMinutes

if now <= graceEnd → ON_TIME
if diffMinutes > veryLateThreshold (120) → VERY_LATE
else → LATE

if (LATE or VERY_LATE) → status = LATE
```

#### ب. تسجيل خروج (`checkOut` - سطر 155)
```
// يجلب الإعدادات:
workEndHour, workEndMinute, earlyLeaveGrace

attendance.checkOutEmployee(currentTime, location, notes, {
  workEndHour, workEndMinute,
  earlyLeaveGracePeriodMinutes
})
```

**`checkOutEmployee` في `Attendance.js` (سطر 216):**
```
workEnd = setHours(workEndHour, workEndMinute)

if now < workEnd → EARLY
else → ON_TIME
(مع فترة سماح: if now >= workEnd AND now < workEnd + grace → ON_TIME)

// يحسب المدة والإضافي عبر calculateDuration():
duration = checkOut - checkIn  (ساعات)
if duration > expectedHours (7) → overtime = duration - 7
else → overtime = 0
```

> **ملاحظة:** الـ `checkOutEmployee` يستخدم `calculateDuration()` من الموديل (سطر 153) الذي يحسب الإضافي كـ `duration - expectedHours`. هذا يعمل بشكل صحيح فقط عندما يكون وقت الدخول = وقت بدء الدوام. للمزامنة من البصمة، يُستخدم `calcDurationOvertime` بدلاً منه.

---

### 3.3 كشف الحضور الشهري (Monthly Timesheet)

#### المصدر: `attendanceController.js` (سطر 895) - `getMonthlyTimesheet`

#### أ. معالجة كل يوم:
```
for each day in month:
  1. تحديد العطل الرسمية (من Holiday model)
  2. تحديد الإجازات المعتمدة (من LeaveRequest model)
  3. جلب سجلات الحضور لذلك اليوم

  // ترتيب الأولوية للحالة:
  if holidayName:
    attendanceStatus = 'holiday'
  else if leaveMatch:
    attendanceStatus = 'on_leave'
  else if no records AND not Friday AND not future:
    attendanceStatus = 'absent'
  else if records exist:
    attendanceStatus = record.status  // present/late/half_day
```

#### ب. حساب التأخير (سطر 1031):
```
checkInMin = checkInTime.getUTCHours() * 60 + getUTCMinutes()
workStartMin = 6 * 60  // 06:00 UTC = 09:00 سعودي

if checkInMin > workStartMin:
  lateHours = (checkInMin - workStartMin) / 60
  isLate = true
  attendanceStatus = 'late'
```

#### ج. حساب دقائق الإضافي (سطر 1047):
```
checkOutMin = checkOutTime.getUTCHours() * 60 + getUTCMinutes()
workEndMin = 13 * 60  // 13:00 UTC = 16:00 سعودي

overtimeMinutes = checkOutMin - workEndMin
if overtimeMinutes > 0:
  overtimeMinutes = الفرق
  isEarlyDeparture = false
```

#### د. الخروج المبكر (سطر 1052):
```
earlyMin = workEndMin - checkOutMin
if earlyMin > 0:
  earlyDepartureMinutes = earlyMin
  isEarlyDeparture = true
```

---

### 3.4 لوحة المشرف (Supervisor Dashboard)

#### المصدر: `backend/controllers/supervisorController.js`

#### أ. تقرير PDF/Excel الحضور (سطر 272-334):
```
mapped = records.map(r => ({
  status: r.status === 'present'     → 'حاضر'
        | r.status === 'absent'      → 'غائب'
        | r.status === 'late'        → 'متأخر'
        | r.status === 'half_day'    → 'نصف يوم'
        | r.status === 'on_leave'    → 'إجازة'
        | r.status === 'work_from_home' → 'عمل عن بعد'
        | else → '-'
}))
```

#### ب. تقرير نشاط الموظف (سطر 508 - `_createEmployeeActivitySheet`):
```
// إحصائيات:
- presentCount, absentCount, lateCount, halfDayCount, leaveCount, wfhCount
- totalDuration, totalOvertime
- missingCheckInCount, missingCheckOutCount, missingBothCount

// معالجة العطل الرسمية:
totalDuration += holidayCount * 7

// معالجة نقص البصمات لكل يوم:
if (!hasCI && !hasCO) → notes = 'نقص البصمتين'
if (!hasCI)          → notes = 'نقص بصمة دخول'
if (!hasCO)          → notes = 'نقص بصمة خروج'
```

---

## 4. الإجازات (Leave)

### المصدر: `backend/controllers/leaveController.js`

### 4.1 نسيان البصمة (`fingerprint_forgotten`)
عند الموافقة على طلب نسيان بصمة (سطر 185-231):

```
dayStart = fingerprintDate منتصف الليل
fpTime   = fingerprintTime (أو 08:00 للدخول / 16:00 للخروج)

if fingerprintType == 'in':
  checkIn.time = fpTime
  checkIn.notes = 'تعويض نسيان بصمة دخول'
else:
  checkOut.time = fpTime
  checkOut.notes = 'تعويض نسيان بصمة خروج'

attendance.status = 'present'
attendance.expectedHours = 7
```

### 4.2 إجازات سنوية/مرضية/طارئة
عند الموافقة (سطر 233-254):

```
for each day from startDate to endDate:
  if day != Friday AND day != Saturday:  // استبعاد الجمعة والسبت
    Attendance.create({
      employee, date,
      status: 'on_leave',
      leave: leaveRequest._id,
      expectedHours: 7,
      duration: isHalfDay ? 4 : 8
    })
```

> **ملاحظة:** الـ `duration` هنا عدد ساعات احتساب الإجازة (8 ساعات لكامل اليوم، 4 لنصف يوم)، وليس وقت التواجد الفعلي.

---

## 5. العطل الرسمية والإجازات (Holidays)

### المصدر: `backend/models/Holiday.js`

```
Holiday {
  name: String,        // اسم العطلة
  startDate: Date,     // تاريخ البداية
  endDate: Date,       // تاريخ النهاية
  type: 'public_holiday' | 'religious' | 'national' | 'other',
  year: Number
}
```

### تأثير العطل على الحسابات:

1. **في تقارير الحضور:** تُحتسب أيام العطل كـ `holiday` في الحالة (`attendanceStatus = 'holiday'`)
2. **في إحصائيات المدة:** تُضاف `7 ساعات` افتراضية لكل يوم عطلة للمدة الإجمالية (`holidayCount * 7`)
3. **في كشف الموظف الشهري:** تُستبعد أيام العطل من `totalWorkingDays`
4. **في الإجازات:** تُستبعد أيام العطل (الجمعة والسبت) من أيام الإجازة المحتسبة

---

## 6. عطلة نهاية الأسبوع

- **الجمعة** (dayOfWeek = 5): يوم عطلة رسمي
- **السبت** (dayOfWeek = 6): يوم عطلة رسمي (في احتساب الإجازات فقط)
- في تقارير الحضور: الجمعة فقط تُعتبر `isWeekend = true` (سطر 949)
- لا تُنشأ سجلات حضور لأيام الجمعة

---

## 7. ملخص تدفق الحساب

```
جهاز البصمة (ZKTeco)
        ↓
    بصمات خام
        ↓
تجميع لكل موظف + يوم
  - أول بصمة = checkIn
  - آخر بصمة = checkOut (بفارق > دقيقة)
        ↓
determineCheckInStatus()
  - ≤ 09:00 → ON_TIME
  - > 09:00 ≤ 120min → LATE
  - > 120min → VERY_LATE
        ↓
hasCheckOut?
  YES → status = ON_TIME ? PRESENT : LATE
  NO  → status = HALF_DAY
        ↓
calcDurationOvertime()
  - المدة = checkOut - checkIn (ساعات)
  - الإضافي = (دقائق قبل 09:00 + دقائق بعد 16:00) / 60
        ↓
حفظ في Attendance (MongoDB)
        ↓
    تقارير
  - Supervisor Dashboard (حاضر/غائب/متأخر/نصف يوم/إجازة)
  - Monthly Timesheet (تأخير + إضافي + خروج مبكر + عطل + إجازات)
  - Excel/PDF تصدير
```
