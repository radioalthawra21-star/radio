# تحليل دمج بيانات Temp-Supervisor مع الموقع

**مصدر الحقيقة النهائي:** `att2000.mdb` بعد دمج `CHECKINOUT` (44,913 بصمة جهاز) + `CHECKEXACT` (عمليات يدوية: إضافة/تعديل/حذف)

**أنواع الحركات:**
- `ISADD=1` ← بصمة أُضيفت يدوياً (غير موجودة في الجهاز ولا CHECKINOUT)
- `ISDELETE=1` ← بصمة حُذفت من التقرير (السجل الأصلي يبقى في CHECKINOUT لكن نخفيه)
- `ISMODIFY=1` ← بصمة عُدّلت (يحتاج اختبار يدوي لتأكيد الآلية — متوقع: حذف + إضافة)

**سلسلة الربط:** `CHECKEXACT.USERID` → `USERINFO.BADGENUMBER` (= `zkUserId`) → نظام المستخدمين → MongoDB

**مشكلة معلومة:** المستخدم 54 (BADGENUMBER=1069) غير معرّف في النظام — إضافاته اليدوية لن تظهر بدون ربط مسبق.

**المخاطر:** تكرار السجلات، تعارض تعديلات Temp-Supervisor مع مزامنة الجهاز، 60 مستخدم في Access مقابل 6 فقط معروفين في النظام.

**أول خطوة:** اختبار ISMODIFY يدوي قبل كتابة أي كود.
