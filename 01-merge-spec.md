# مواصفة دمج CHECKEXACT مع CHECKINOUT

## 1. هيكل الجدولين والعلاقة بينهما

### CHECKINOUT (بيانات الجهاز الخام)
| الحقل | النوع | المعنى |
|-------|-------|--------|
| `USERID` | int (4) | معرف المستخدم الداخلي في Access |
| `CHECKTIME` | datetime (8) | تاريخ ووقت البصمة |
| `CHECKTYPE` | text(1) | I = دخول / O = خروج |
| `VERIFYCODE` | int | طريقة التحقق (1=بصمة، 2=وجه...) |
| `SENSORID` | text(5) | معرف القارئ |
| `Memoinfo` | text(30) | ملاحظات |
| `WorkCode` | text(24) | كود العمل |
| `sn` | text(20) | الرقم التسلسلي للجهاز (CGCA211460361) |
| `UserExtFmt` | int | تنسيق إضافي |

- لا يوجد مفتاح أساسي (Primary Key) عمود واحد.
- المفتاح المركب الطبيعي هو `(USERID + CHECKTIME + CHECKTYPE)`.
- جميع السجلات الـ 44,913 تحمل `sn = CGCA211460361` ← كلها من الجهاز.

### CHECKEXACT (سجل العمليات يدوية)
| الحقل | النوع | المعنى |
|-------|-------|--------|
| `EXACTID` | int (PK) | المعرف التسلسلي التلقائي |
| `USERID` | int | معرف المستخدم في Access |
| `CHECKTIME` | datetime | **القيمة الجديدة** للبصمة |
| `CHECKTYPE` | text(1) | I = دخول / O = خروج |
| `ISADD` | bit (0/1) | هل هذه إضافة يدوية؟ |
| `ISMODIFY` | bit (0/1) | هل هذا تعديل على سجل موجود؟ |
| `ISDELETE` | bit (0/1) | هل هذا حذف لسجل موجود؟ |
| `YUYIN` | text | ملاحظات صوتية/نصية عربية (قد تكون مشوشة) |
| `INCOUNT` | bit | يُحتسب ضمن الدخول |
| `ISCOUNT` | bit | يُحتسب ضمن الإجمالي |
| `MODIFYBY` | text | من قام بالعملية ("جهاز البصمة") |
| `DATE` | datetime | تاريخ ووقت تنفيذ العملية |

### المفتاح الرابط بين الجدولين
**لا يوجد مفتاح أجنبي (Foreign Key) مباشر.** لا يحتوي `CHECKINOUT` على `EXACTID`.

الربط يتم عبر المفتاح المركب الطبيعي: **`USERID + CHECKTYPE + CHECKTIME`**

للإضافة: `CHECKEXACT.ISADD = 1` ← السجل غير موجود في `CHECKINOUT` (تم التأكيد — المستخدمين 1 و 54 لا يوجد لهما سجلات في `CHECKINOUT` بتاريخ 10/6/2026 مع أن `CHECKEXACT` يظهر إضافات لهما).

للحذف: `CHECKEXACT.ISDELETE = 1` ← السجل الأصلي في `CHECKINOUT` يُستبعد بمطابقة `(USERID + CHECKTIME + CHECKTYPE)` الأصليين.

للتعديل: يحتاج توضيح (انظر أدناه ⚠️).

---

## 2. منطق الدمج التفصيلي

### 2.1 الإضافة (`ISADD = 1`)

```
لكل سجل E في CHECKEXACT حيث E.ISADD = 1:
    أضف (USERID, CHECKTIME, CHECKTYPE) من E إلى النتيجة النهائية.
```

- القصة: المستخدم ضغط "إضافة" في جهاز البصمة وأدخل بصمة يدوية لموظف.
- السجل **غير موجود** في `CHECKINOUT` لأنه لم يأتِ من جهاز البصمة.
- الإضافة تُدرَج في النتيجة النهائية بجانب سجلات الجهاز.

مثال واقعي:
```
CHECKEXACT: USERID=1, CHECKTIME=6/10/2026 09:00, CHECKTYPE=I, ISADD=1
CHECKINOUT: (لا يوجد سجل للمستخدم 1 في هذا التاريخ)
النتيجة: يُضاف سجل دخول 9:00 للمستخدم 1
```

### 2.2 الحذف (`ISDELETE = 1`)

```
لكل سجل D في CHECKEXACT حيث D.ISDELETE = 1:
    استبعد من CHECKINOUT كل سجل C حيث:
        C.USERID = D.USERID
        C.CHECKTYPE = D.CHECKTYPE
        C.CHECKTIME = D.CHECKTIME
```

- القصة: المستخدم اختار بصمة معينة في جهاز البصمة وضغط Delete.
- السجل الأصلي يبقى في `CHECKINOUT` (لأنه من الجهاز ولا نمسح بيانات الجهاز).
- ولكن علامة ISDELETE في CHECKEXACT تخبرنا أن نُخفي هذا السجل من النتيجة.

### 2.3 التعديل (`ISMODIFY = 1`) — ⚠️ يحتاج توثيق ميداني

#### المشكلة
عند تعديل بصمة في جهاز البصمة، تتغير قيمة `CHECKTIME` أو `CHECKTYPE`. ولكن `CHECKEXACT` يخزن القيمة **الجديدة فقط** وليس لديه عمود للقيمة القديمة. في المقابل، `CHECKINOUT` يخزن القيمة **الأصلية من الجهاز**.

إذا المفتاح الرابط هو `(USERID + CHECKTIME + CHECKTYPE)`، والقيمة الجديدة تختلف عن القيمة القديمة، **فكيف نجد السجل الأصلي لنستبدله؟**

#### النموذج المرجح (يحتاج اختبار)

```
للتعديل: ننشئ سجلين في CHECKEXACT:
  1. واحد بـ ISDELETE=1 يحمل القيم OLD (USERID + CHECKTIME_OLD + CHECKTYPE_OLD)
  2. واحد بـ ISADD=1 يحمل القيم NEW (USERID + CHECKTIME_NEW + CHECKTYPE_NEW)
```

في هذه الحالة:
- `ISMODIFY` هو مجرد علامة تربط بين الحذف والإضافة في عملية واحدة (علم تجميعي).
- `ISMODIFY=1` يحمل القيم الجديدة، لكن عملية الحذف للقيم القديمة منفصلة في سجل CHECKEXACT آخر.

> 🧪 **مطلوب اختبار**: إنشاء تعديل يدوي في جهاز البصمة ثم تفريغ CHECKEXACT للتأكد من ظهور سجل ISDELETE + سجل ISADD معاً.

**احتمال بديل**: ربما جهاز البصمة يُحدث CHECKINOUT في المكان نفسه (modify in place) ويسجل في CHECKEXACT للتوثيق فقط. في هذه الحالة، `ISMODIFY=1` لا يتطلب أي منطق دمج إضافي — `CHECKINOUT` يحتوي القيمة المعدلة بالفعل، ونحن فقط نقرأ `CHECKINOUT`. يحتاج اختبار للتأكيد.

---

## 3. خوارزمية الدمج الكاملة (Merge Algorithm)

```
1. قراءة كل سجلات CHECKINOUT
2. قراءة كل سجلات CHECKEXACT
3. بناء النتيجة النهائية:

   أ. ابدأ مع CHECKINOUT كقاعدة (result = CHECKINOUT)
   
   ب. طبق الحذف:
      لكل D في CHECKEXACT حيث D.ISDELETE = 1:
          احذف من result: (USERID = D.USERID و CHECKTYPE = D.CHECKTYPE و CHECKTIME = D.CHECKTIME)
          (تأكد من حذف سجل واحد فقط في حالة التكرار)
   
   ج. طبق التعديل (إذا ISMODIFY يُستخدم كـ replace in place):
      لكل M في CHECKEXACT حيث M.ISMODIFY = 1:
          السجل الأصلي: (USERID = M.USERID, CHECKTYPE = M.CHECKTYPE, CHECKTIME = M.CHECKTIME_ORIGINAL)
          ⚠️ CHECKTIME_ORIGINAL غير معروف — يحتاج إيضاح
          الحل المؤقت: تجاهل التعديلات (ISMODIFY) في المرحلة الأولى
   
   د. طبق الإضافة:
      لكل A في CHECKEXACT حيث A.ISADD = 1:
          أضف إلى result: (USERID = A.USERID, CHECKTYPE = A.CHECKTYPE, CHECKTIME = A.CHECKTIME)
          منع التكرار: إذا وجد سجل بنفس (USERID + CHECKTYPE + CHECKTIME) بالفعل في result، لا نضيفه ثانية
   
4. النتيجة النهائية تحتوي على:
      - سجلات CHECKINOUT الأصلية (ما لم تُحذف)
      - سجلات CHECKINOUT المعدلة (بعد تطبيق التعديلات)
      - سجلات CHECKEXACT المضافة يدوياً
      - ⛔ باستثناء السجلات المحذوفة (ISDELETE)
```

### منع التكرار
عند الإضافة (`ISADD`)، قد يحدث أن المستخدم أضاف بصمة يدوية تطابق بالصدفة بصمة موجودة أصلاً من الجهاز. لمنع التكرار:
```
قبل إضافة A إلى النتيجة:
    تحقق: هل يوجد سجل في النتيجة بنفس (USERID, CHECKTIME, CHECKTYPE)؟
    إذا نعم: ← لا تُضف A (الأولوية للجهاز أو الأولوية للسجل الموجود)
    إذا لا: ← أضف A
```

---

## 4. تطابق النتائج مع جهاز البصمة

للتأكد من أن الموقع يعرض نفس النتائج التي يعرضها جهاز البصمة، يجب اتباع نفس خوارزمية الدمج التي يستخدمها البرنامج الأصلي.

### خطة التحقق

| الخطوة | ماذا نفعل | كيف نتأكد |
|--------|-----------|-----------|
| 1 | فتح جهاز البصمة، تصفية تقرير ليوم معين | تصوير النتائج |
| 2 | تشغيل خوارزمية الدمج على نفس اليوم | مقارنة عدد السجلات وتواقيتها |
| 3 | تنفيذ إضافة يدوية في جهاز البصمة | ظهورها في الموقع بعد التزامن التالي |
| 4 | تنفيذ تعديل في جهاز البصمة | تفريغ CHECKEXACT لرؤية ISMODIFY + ISDELETE |
| 5 | تنفيذ حذف في جهاز البصمة | اختفاء البصمة من الموقع بعد التزامن |

**معيار القبول:**
- لكل يوم، نفس عدد البصمات لكل موظف.
- نفس توقيت البصمات (دقيقة بدقيقة).
- السجلات المضافة يدوياً تظهر في كلا النظامين.
- السجلات المحذوفة تختفي من كلا النظامين.

---

## 5. متى يتم استبدال/إضافة/إخفاء

| العملية | متى تُطبَّق | التأثير على CHECKINOUT | التأثير على النتيجة النهائية |
|---------|-------------|----------------------|----------------------------|
| ISADD | البصمة غير موجودة في الجهاز أصلاً | لا يوجد سجل مقابل | سطر جديد يُضاف |
| ISMODIFY | بصمة موجودة في الجهاز وتعديل وقتها/نوعها | السجل الأصلي موجود في CHECKINOUT | السجل الأصلي يُستبدل (يُحذف ثم يُضاف الجديد) |
| ISDELETE | بصمة موجودة في الجهاز ونريد إخفاءها | السجل الأصل موجود في CHECKINOUT | السجل يُستبعد من النتيجة |
| لا شيء | بصمة عادية من الجهاز | موجود في CHECKINOUT | يُعرض كما هو |

---

## 6. الخلاصة

| البند | القيمة |
|-------|--------|
| مصدر بيانات الجهاز | `CHECKINOUT` (44,913 سجل، كلها تحمل sn) |
| مصدر الحركات اليدوية | `CHECKEXACT` (حالياً 3 إضافات ISADD فقط) |
| المفتاح الرابط | `(USERID, CHECKTYPE, CHECKTIME)` — مركب طبيعي لا FK |
| حالة ISMODIFY | **غير مؤكدة** — يحتاج اختبار ميداني (انظر §2.3) |
| حالة ISDELETE | لا توجد سجلات حذف بعد — المنطق واضح بالمطابقة الثلاثية |
| منع التكرار | التحقق من وجود `(USERID, CHECKTYPE, CHECKTIME)` قبل إدراج أي سجل |

---

## 7. خلاصة التحقيق (مُلحق)

| البند | الوصف |
|-------|-------|
| مصدر `CHECKINOUT` | بيانات الجهاز فقط — كل 44,913 سجل تحمل رقم جهاز `CGCA211460361` |
| مصدر `CHECKEXACT` | العمليات اليدوية فقط — إضافة/تعديل/حذف |
| المفتاح الرابط | `(USERID + CHECKTYPE + CHECKTIME)` — لا يوجد FK مباشر بين الجدولين |
| `ISADD = 1` | السجل غير موجود في `CHECKINOUT` أصلاً (تأكدنا: USERID=1 و 54 لا يوجد لهما بصمات اليوم في `CHECKINOUT` مع أن `CHECKEXACT` يظهر إضافات لهما) |
| `ISDELETE = 1` | استبعاد السجل من `CHECKINOUT` بالمطابقة الثلاثية `(USERID, CHECKTYPE, CHECKTIME)` |
| `ISMODIFY = 1` | ⚠️ **يحتاج اختبار ميداني** — حالياً لا توجد سجلات تعديل. الاحتمال المرجح: جهاز البصمة يُنشئ سجلين (ISDELETE + ISADD) عند التعديل |
| ما هو مؤكد | `CHECKINOUT` يعطي فقط بصمات الجهاز الخام. `CHECKEXACT` يعطي فقط التعديلات اليدوية. الموقع الحالي يقرأ الجهاز فقط ← لا يرى أي تعديل من جهاز البصمة |

**الاستنتاج:** لضمان تطابق الموقع مع جهاز البصمة، يجب دمج `CHECKEXACT` مع `CHECKINOUT` وفق الخوارزمية المذكورة أعلاه. أما `ISMODIFY` فحتاج اختبار عملي لتأكيد آلية الربط مع السجل الأصلي.
