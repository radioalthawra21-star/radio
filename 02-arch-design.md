# تقرير التصميم النهائي: دمج تعديلات جهاز البصمة مع الموقع

## 1. تحليل الوضع الراهن

### 1.1 المصادر الحالية للبيانات

```
┌─────────────────────────────────────────────────────────────────┐
│                         att2000.mdb                            │
│  ┌──────────────────────┐  ┌──────────────────────────────────┐ │
│  │ CHECKINOUT (44,913)   │  │ CHECKEXACT (3 سجلات)            │ │
│  │ جميعها من الجهاز (sn) │  │ USERID→BADGENUMBER→zkUserId     │ │
│  │ USERID= داخلي في DB  │  │ 1→1016→ابو السعد ✓              │ │
│  │ BADGENUMBER=zkUserId │  │ 54→1069→غير معروف ❌             │ │
│  └──────────────────────┘  └──────────────────────────────────┘ │
│  USERINFO: 60 مستخدم, BADGENUMBER يطابق zkUserId               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      جهاز ZKTeco                               │
│  ذاكرة داخلية: ~44K بصمة (يتغير باستمرار)                       │
│  متصل عبر TCP:192.168.15.50:4370                                │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      MongoDB Atlas                             │
│  Attendance model: employee + date + checkIn/checkOut + status  │
│  يحتوي فقط على البصمات القادمة من Bridge (الجهاز)              │
│  لا يحتوي على أي تعديلات من جهاز البصمة                     │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 الفجوة

الموقع يعرض بصمات الجهاز فقط. جهاز البصمة يخزّن تعديلاته في `att2000.mdb\CHECKEXACT` حصراً.
الموقع لا يقرأ `CHECKEXACT` ← التعديلات لا تظهر أبداً.

### 1.3 خريطة الربط (USERID → BADGENUMBER → zkUserId)

كل سجل في `CHECKINOUT` و `CHECKEXACT` يحمل `USERID` (رقم داخلي في Access).
`USERINFO` يربط `USERID` ← `BADGENUMBER` (وهو zkUserId الفعلي في النظام).

```
مثال:
  CHECKEXACT.USERID=1 → USERINFO.BADGENUMBER=1016 → zkUserId=1016 → ابو السعد ✓
  CHECKEXACT.USERID=54 → USERINFO.BADGENUMBER=1069 → zkUserId=1069 → غير معروف ❌
```

عدد المستخدمين الموجودين في USERINFO: 60 مستخدم.
عدد المستخدمين المعروفين في النظام (ذوو zkUserId): 6 مستخدمين فقط.
**أي سجل لـ USERID غير معرّف zkUserId في النظام سيفشل ربطه.**

---

## 2. أنواع الحركات اليدوية المطلوب دعمها

### 2.1 ISADD = 1 — إضافة يدوية

| الخانة | القيمة |
|--------|--------|
| المعنى | مستخدم أضاف بصمة يدوياً عبر جهاز البصمة |
| مكان التخزين | `CHECKEXACT` فقط — غير موجود في CHECKINOUT ولا الجهاز |
| المفتاح | `(USERID, CHECKTYPE, CHECKTIME)` |
| عدد السجلات حالياً | 3 سجلات (للمستخدمين 1 و 54) |
| طريقة الدمج | إضافة سجل حضور جديد في MongoDB |
| منع التكرار | التأكد من عدم وجود نفس `(zkUserId + CHECKTIME)` في MongoDB |

### 2.2 ISDELETE = 1 — حذف بصمة

| الخانة | القيمة |
|--------|--------|
| المعنى | مستخدم حذف بصمة من تقرير الحضور |
| مكان التخزين | سجل في CHECKEXACT مع ISDELETE=1 + القيم الأصلية |
| المفتاح الرابط مع CHECKINOUT | `(USERID, CHECKTYPE, CHECKTIME)` الأصلي |
| عدد السجلات حالياً | 0 |
| طريقة الدمج | إيجاد سجل الحضور المطابق في MongoDB وحذفه أو تعطيله |
| ملاحظة | السجل يبقى في CHECKINOUT — نحن فقط نخفيه من النتيجة |

### 2.3 ISMODIFY = 1 — تعديل بصمة ⚠️

| الخانة | القيمة |
|--------|--------|
| المعنى | مستخدم عدّل وقت/نوع بصمة موجودة |
| مكان التخزين | يحتاج اختبار (انظر §2.3 في 01-merge-spec.md) |
| الآلية المرجحة | جهاز البصمة يُنشئ ISDELETE للسجل القديم + ISADD للسجل الجديد |
| عدد السجلات حالياً | 0 |
| طريقة الدمج المبدئية | معاملة ISMODIFY كـ: إزالة السجل الأصلي + إضافة السجل المعدّل |
| تعليق | **يتطلب اختباراً يدوياً لتأكيد الآلية** قبل البرمجة |

---

## 3. مصدر الحقيقة النهائي (Source of Truth)

### 3.1 الهرم المرجعي

```
الترتيب من الأعلى سلطةً إلى الأدنى:
┌─────────────────────────────────────────────────────────────┐
│  1. att2000.mdb (CHECKINOUT + CHECKEXACT) ← المرجع الأساسي  │
│     لأنه يحتوي: بيانات الجهاز + جميع التعديلات اليدوية      │
├─────────────────────────────────────────────────────────────┤
│  2. جهاز ZKTeco ← المرجع للبصمات الجديدة (real-time)        │
│     (تُضاف إلى CHECKINOUT لاحقاً عبر جهاز البصمة)       │
├─────────────────────────────────────────────────────────────┤
│  3. MongoDB ← النسخة المخزّنة (cache) التي يقرأها الموقع   │
│     يجب أن تعكس رقم 1 بعد الدمج                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 لماذا `att2000.mdb` هو المرجع الأساسي؟

1. يحتوي **كل** بصمات الجهاز (`CHECKINOUT` بجميع سجلاتها الـ 44,913).
2. يحتوي **كل** التعديلات اليدوية (`CHECKEXACT`: إضافة/تعديل/حذف).
3. الجهاز نفسه قد يحذف السجلات القديمة من ذاكرته (يتغير باستمرار).
4. جهاز البصمة يزامن الجهاز مع `CHECKINOUT` دورياً ← `att2000.mdb` محدّث.

### 3.3 المبدأ

```
MongoDB = f(CHECKINOUT, CHECKEXACT)

حيث f هي:
  1. ابدأ مع CHECKINOUT
  2. استبعد أي سجل يقابله ISDELETE في CHECKEXACT
  3. أضف أي سجل ISADD في CHECKEXACT
  4. طبق ISMODIFY (عند تأكيد الآلية)
```

---

## 4. التعديلات المطلوبة على مستوى التصميم فقط

### 4.1 مسار بيانات جديد

```
                     ┌──────────────────┐
                     │  att2000.mdb     │
                     │  (Access DB)     │
                     └────┬──────┬──────┘
                          │      │
                    ┌─────┘      └─────┐
                    ▼                  ▼
             ┌────────────┐   ┌──────────────┐
             │ CHECKINOUT │   │ CHECKEXACT   │
             │ (44,913)   │   │ (عمليات)     │
             └──────┬─────┘   └──────┬───────┘
                    │                │
                    └──────┬─────────┘
                           │ دمج
                           ▼
                    ┌──────────────┐
                    │ Merge Logic  │
                    │ (جديد)       │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │   MongoDB    │
                    │ Attendance   │
                    └──────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │   Website    │
                    └──────────────┘

     (يُضاف كمسار موازٍ لمسار الجهاز الحالي)
```

### 4.2 المكونات الجديدة المطلوبة

| المكون | الوظيفة | مكانه المقترح |
|--------|---------|--------------|
| **قارئ att2000.mdb** | قراءة CHECKINOUT و CHECKEXACT من Access DB | `bridge/read-att2000.js` |
| **مخدم التحويل USERID→zkUserId** | ترجمة USERID عبر USERINFO.BADGENUMBER | `bridge/user-mapper.js` |
| **منطق الدمج** | تطبيق CHECKEXACT على CHECKINOUT | `bridge/merge-logic.js` |
| **مزامن MongoDB** | مقارنة النتيجة مع MongoDB وتحديثها | `bridge/mongo-sync.js` |

### 4.3 تدفق المزامنة الجديد (Sync Flow)

```
1. قراءة CHECKINOUT من att2000.mdb
2. قراءة CHECKEXACT من att2000.mdb  
3. قراءة USERINFO من att2000.mdb (للربط USERID→BADGENUMBER)
4. تحويل USERID الداخلي → zkUserId → ObjectId (User في MongoDB)
5. تطبيق CHECKEXACT على CHECKINOUT (الدمج)
6. مقارنة النتيجة مع Attendance في MongoDB
7. إدراج/تحديث/حذف السجلات في MongoDB حسب الفرق
```

### 4.4 التعامل مع المستخدمين غير المعرّفين

سجلات لـ `USERID` ليس له `zkUserId` معرف في النظام:
- **خيار أ**: تجاهلها (لا تُضاف إلى MongoDB)
- **خيار ب**: إضافتها مع `deviceUserId` و `deviceUserName` (حتى لو لم يُربط الموظف بعد)
- **خيار ج**: إضافتها إلى جدول منفصل "غير معرّفين" لمراجعتها لاحقاً

**التوصية**: الخيار ب — تضاف إلى MongoDB مع `employee=null` و `deviceUserId=BADGENUMBER`
لتظهر لاحقاً عند ربط المستخدم.

### 4.5 التعامل مع بصمات الجهاز الجديدة (real-time)

جهاز البصمة يستقبل بصمات جديدة يومياً. Bridge الحالي يقرأها من الجهاز.
عند إضافة مسار `att2000.mdb`:

- **مشكلة**: البصمات الجديدة تظهر في الجهاز فوراً، لكنها لا تصل إلى `att2000.mdb` إلّا بعد مزامنة جهاز البصمة مع الجهاز (قد تتأخر دقائق أو ساعات).
- **حل**: الإبقاء على قراءة الجهاز (Bridge الحالي) للبصمات الجديدة + قراءة `att2000.mdb` للتعديلات. الدمج النهائي:
  ```
  النتيجة النهائية = MAX(بيانات الجهاز, بيانات att2000.mdb بعد الدمج)
  ```
  أي: إذا كان السجل موجوداً في المصدرين، نأخذ آخر تحديث زمنياً.

---

## 5. خطة التنفيذ التفصيلية

### المرحلة 1: الأساسيات (تحقق + إثبات)

| الخطوة | الوصف | المخرجات |
|--------|-------|----------|
| 1.1 | اختبار ISMODIFY يدوي: تعديل بصمة في جهاز البصمة ← تفريغ CHECKEXACT | تأكيد آلية ISMODIFY (delete+add أم replace in place) |
| 1.2 | اختبار ISDELETE يدوي: حذف بصمة في جهاز البصمة ← تفريغ CHECKEXACT | تأكيد بنية سجل الحذف |
| 1.3 | برهان مفهوم (POC): قراءة CHECKEXACT من PowerShell وإرسالها للـ API | إثبات أن القراءة من Access ممكنة والبيانات تصل للموقع |
| 1.4 | فتح اتصال Access في PowerShell خارجياً مع جهاز البصمة قيد التشغيل | التأكد من عدم قفل الملف |

### المرحلة 2: مكون قراءة att2000.mdb

| الخطوة | الوصف | المخرجات |
|--------|-------|----------|
| 2.1 | إنشاء وحدة قراءة CHECKINOUT (PowerShell أو Node.js مع mdb-reader) | Array من سجلات CHECKINOUT |
| 2.2 | إنشاء وحدة قراءة CHECKEXACT | Array من سجلات CHECKEXACT |
| 2.3 | إنشاء وحدة قراءة USERINFO (USERID→BADGENUMBER) | Map: USERID → zkUserId |
| 2.4 | معالجة الأخطاء: ملف مقفول، اتصال فاشل، تنسيق زمني | تقارير خطأ واضحة |

### المرحلة 3: منطق الدمج

| الخطوة | الوصف | المخرجات |
|--------|-------|----------|
| 3.1 | تطبيق ISDELETE: استبعاد من CHECKINOUT | CHECKINOUT بدون المحذوفات |
| 3.2 | تطبيق ISADD: إضافة السجلات الجديدة | CHECKINOUT + ISADD |
| 3.3 | تطبيق ISMODIFY (وفق الآلية المُختبرة) | CHECKINOUT + ISADD + ISMODIFY |
| 3.4 | إزالة التكرار: (USERID, CHECKTYPE, CHECKTIME) مكرر | قائمة فريدة |
| 3.5 | تحويل USERID → BADGENUMBER → zkUserId → system User | سجلات بمعرفات النظام |

### المرحلة 4: المزامنة مع MongoDB

| الخطوة | الوصف | المخرجات |
|--------|-------|----------|
| 4.1 | مقارنة قائمة الدمج مع Attendance في MongoDB | الفرق (إضافات/تحديثات/حذف) |
| 4.2 | إدراج السجلات الجديدة في MongoDB | سجلات جديدة بالموقع |
| 4.3 | تحديث السجلات المعدلة في MongoDB | توقيتات/حالات محدّثة |
| 4.4 | إخفاء/حذف السجلات المحذوفة في MongoDB | اختفاء البصمات المحذوفة |
| 4.5 | معالجة السجلات بدون zkUserId: تخزين مع flag | deviceUserId, deviceUserName |

### المرحلة 5: الجدولة والتكامل

| الخطوة | الوصف | المخرجات |
|--------|-------|----------|
| 5.1 | دمج مسار الدمج مع Bridge Service الحالي | عملية مزامنة موحّدة |
| 5.2 | تحديد تردد المزامنة: كل دقيقة؟ كل 5 دقائق؟ | إعدادات قابلة للتعديل |
| 5.3 | إضافة سجل (log) لكل عملية مزامنة | تتبع تاريخ التغييرات |
| 5.4 | إضافة endpoint في API: `GET /api/zkteco/merge-status` | مراقبة حالة الدمج |

### المرحلة 6: الاختبار والتحقق

| الخطوة | الوصف | المخرجات |
|--------|-------|----------|
| 6.1 | مقارنة يدوية: موقع vs جهاز البصمة لنفس اليوم | تطابق 100% |
| 6.2 | اختبار إضافة بصمة يدوية في جهاز البصمة | ظهورها في الموقع خلال دقيقة |
| 6.3 | اختبار حذف بصمة في جهاز البصمة | اختفاؤها من الموقع |
| 6.4 | اختبار تعديل بصمة في جهاز البصمة | تغير الوقت/النوع في الموقع |
| 6.5 | اختبار المستخدمين غير المعرّفين | لا تظهر في التقارير ولكن تُسجل |

---

## 6. المخاطر المحتملة والحلول

### 6.1 تكرار السجلات (Duplicate Records)

| السيناريو | المشكلة | الحل |
|-----------|---------|------|
| نفس البصمة في الجهاز + CHECKEXACT.ISADD | يظهر سجلان لنفس الموظف في نفس الوقت | التحقق من `(zkUserId, CHECKTIME)` قبل الإدراج |
| مزامنة الجهاز + مزامنة CHECKEXACT في نفس الوقت | تضارب أثناء المقارنة | استخدام طابع زمني (timestamp) وقفل (mutex) |
| جهاز البصمة يضيف بصمة مكررة | سجل مكرر في CHECKEXACT | تجاهل EXACTID المكرر، الاحتفاظ بالأحدث |

### 6.2 تعارض السجلات (Conflicting Records)

| السيناريو | المشكلة | الحل |
|-----------|---------|------|
| بصمة على الجهاز الساعة 9:00، وفي جهاز البصمة عُدّلت إلى 8:30 | من يأخذ الأولوية؟ | `att2000.mdb` هو المرجع (كما يفعل جهاز البصمة) |
| بصمة محذوفة في جهاز البصمة، ثم يعيد الجهاز إرسالها | تعود للظهور | حذفها من المزامنة إذا كان `ISDELETE` موجوداً |
| تعديل وقت البصمة في جهاز البصمة بعد مزامنة Bridge لها | السجل القديم في MongoDB + السجل الجديد في CHECKEXACT | تحديث السجل الموجود بدلاً من إنشاء جديد |

### 6.3 مشاكل الربط (Mapping Issues)

| السيناريو | المشكلة | الحل |
|-----------|---------|------|
| `BADGENUMBER` في USERINFO لا يطابق أي `zkUserId` | سجلات لا يمكن ربطها بموظف | تخزين مؤقت مع `deviceUserId` لحين الربط |
| مستخدم معرّف في الموقع لكن ليس في USERINFO | بصماته غير موجودة في Access | لا تؤثر — موجودة على الجهاز وتُقرأ عبر Bridge |
| `BADGENUMBER` مكرر في USERINFO | التباس في الربط | أخذ أول سجل فقط وتسجيل تحذير |

### 6.4 مشاكل وصول Access DB

| السيناريو | المشكلة | الحل |
|-----------|---------|------|
| جهاز البصمة يفتح `att2000.mdb` حصرياً | فشل في القراءة | فتح للقراءة فقط (`ReadOnly=true`) |
| المستخدم يُغلق جهاز البصمة | `att2000.mdb` لا يُحدّث بعدها | البصمات الجديدة لن تظهر في Access (لكن تبقى على الجهاز) |
| تلف ملف `att2000.mdb` | فشل في قراءة البيانات نهائياً | الرجوع إلى قراءة الجهاز فقط (fallback) |

### 6.5 أداء وتوقيت

| السيناريو | المشكلة | الحل |
|-----------|---------|------|
| قراءة 44,913 سجل في كل دورة | بطء وتحميل على النظام | استخدام Cursor: قراءة السجلات الجديدة فقط منذ آخر مزامنة |
| استعلام JOIN بين CHECKEXACT و CHECKINOUT | بطء استعلام Access | معالجة منفصلة ثم دمج في الذاكرة |
| Bridge Service يقرأ من Access والجهاز معاً | تعقيد زائد | فصل المسارين: مزامنة دورية لـ Access + مزامنة مستمرة للجهاز |

---

## 7. توصيات نهائية

### 7.1 الأولويات

1. **قبل أي كود**: تنفيذ اختبار ISMODIFY يدوي (الخطوة 1.1)
2. **أولوية قصوى**: برهان مفهوم (POC) — قراءة CHECKEXACT من PowerShell وإرسالها للـ API
3. **أولوية عالية**: معالجة المستخدمين غير المعرّفين (Badge=1069 وغيره)
4. **أولوية متوسطة**: دمج المسارين (att2000.mdb + الجهاز)
5. **أولوية منخفضة**: الجدولة الآلية والمراقبة

### 7.2 قرارات هندسية مفتوحة

| القرار | الخيارات | يُقرّر بعد |
|--------|---------|-----------|
| لغة قراءة Access DB | PowerShell SDK Bridge | اختبار الأداء |
| | Node.js مع `mdb-reader` npm | |
| | ADODB عبر child_process | |
| تردد المزامنة | كل دقيقة (للتعديلات) | ملاحظة تأثير الأداء |
| | كل 5 دقائق (افتراضي) | |
| | عند الطلب فقط | |
| التعامل مع ISMODIFY | delete+add (المتوقع) | الاختبار اليدوي (1.1) |
| | replace in-place (احتمال) | |
| Fallback عند فشل Access DB | تجاهل CHECKEXACT والرجوع للجهاز فقط | — |
| المستخدمين غير المعرّفين | تجاهل | نقاش مع الفريق |
| | تخزين مع deviceUserId | |

### 7.3 المبدأ التوجيهي

> **لا تُجرِ أي تعديل على قاعدة البيانات الأصلية (att2000.mdb).**
> اقرأ فقط (Read-Only) وطبّق التغييرات على MongoDB.
> جهاز البصمة هو المالك الحصري لـ att2000.mdb.

---

## الملحق: خريطة مراحل التنفيذ الكاملة

```
المرحلة 1 ──► اختبار ISMODIFY + برهان مفهوم (POC)
    │
    ▼
المرحلة 2 ──► بناء قارئ att2000.mdb
    │
    ▼
المرحلة 3 ──► بناء منطق الدمج (CHECKINOUT + CHECKEXACT)
    │
    ▼
المرحلة 4 ──► بناء مزامن MongoDB
    │
    ▼
المرحلة 5 ──► التكامل مع Bridge Service + الجدولة
    │
    ▼
المرحلة 6 ──► الاختبار والمقارنة مع جهاز البصمة
    │
    ▼
      اكتمال — التعديلات تظهر في الموقع ⏺
```
